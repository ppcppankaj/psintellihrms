# 🏗️ ARCHITECTURE DIAGRAMS - Schema vs Organization-Based

## Current Architecture (Schema-Based) ❌

```
┌─────────────────────────────────────────────────────────────┐
│                    PostgreSQL Database                       │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ Schema: public                                        │  │
│  ├───────────────────────────────────────────────────────┤  │
│  │ • Tenant (id, name, slug, schema_name)               │  │
│  │ • Domain (subdomain mapping)                          │  │
│  │ • User (shared? or per-schema?)                       │  │
│  │ • Billing (subscription plans)                        │  │
│  └───────────────────────────────────────────────────────┘  │
│                                                               │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ Schema: acme_corp                                     │  │
│  ├───────────────────────────────────────────────────────┤  │
│  │ • employees_employee                                   │  │
│  │ • payroll_payrollrun                                  │  │
│  │ • attendance_attendancerecord                         │  │
│  │ • leave_leaverequest                                  │  │
│  │ • ... (all tables duplicated)                         │  │
│  └───────────────────────────────────────────────────────┘  │
│                                                               │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ Schema: globex_inc                                    │  │
│  ├───────────────────────────────────────────────────────┤  │
│  │ • employees_employee                                   │  │
│  │ • payroll_payrollrun                                  │  │
│  │ • attendance_attendancerecord                         │  │
│  │ • ... (all tables duplicated again)                   │  │
│  └───────────────────────────────────────────────────────┘  │
│                                                               │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ Schema: initech                                       │  │
│  ├───────────────────────────────────────────────────────┤  │
│  │ • employees_employee                                   │  │
│  │ • ... (all tables duplicated again)                   │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘

Request Flow:
─────────────
1. HTTP Request: https://acme-corp.hrms.com/api/employees/
2. Middleware extracts subdomain: "acme-corp"
3. Lookup Tenant by slug → schema_name = "acme_corp"
4. connection.set_schema("acme_corp")
5. All queries run in acme_corp schema
6. User in acme_corp CANNOT see globex_inc data (different schema)

Problems:
─────────
❌ Complex schema routing
❌ Migrations must run per schema (slow)
❌ Cross-tenant analytics difficult
❌ Connection pool per schema
❌ Not all databases support schemas (MySQL, SQLite)
❌ Harder debugging (which schema am I in?)
❌ django-tenants dependency
```

---

## Target Architecture (Organization-Based) ✅

```
┌─────────────────────────────────────────────────────────────┐
│              PostgreSQL Database (Single Schema)             │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ core_organization                                     │  │
│  ├───────────────────────────────────────────────────────┤  │
│  │ id (UUID) | name        | slug       | is_active     │  │
│  ├───────────┼─────────────┼────────────┼───────────────┤  │
│  │ uuid-1    | ACME Corp   | acme-corp  | true          │  │
│  │ uuid-2    | Globex Inc  | globex-inc | true          │  │
│  │ uuid-3    | Initech     | initech    | true          │  │
│  └───────────────────────────────────────────────────────┘  │
│                                                               │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ auth_user                                             │  │
│  ├───────────────────────────────────────────────────────┤  │
│  │ id | email          | organization_id | is_active    │  │
│  ├────┼────────────────┼─────────────────┼──────────────┤  │
│  │ 1  | admin@acme.com | uuid-1          | true         │  │
│  │ 2  | john@globex.io | uuid-2          | true         │  │
│  │ 3  | admin@acme.com | uuid-2          | true  ← Same email, diff org! │
│  └───────────────────────────────────────────────────────┘  │
│  UNIQUE (organization_id, email)                             │
│                                                               │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ employees_employee                                     │  │
│  ├───────────────────────────────────────────────────────┤  │
│  │ id | organization_id | user_id | employee_id | ...   │  │
│  ├────┼─────────────────┼─────────┼─────────────┼───────┤  │
│  │ 1  | uuid-1          | 1       | EMP001      | ...   │  │
│  │ 2  | uuid-2          | 2       | EMP001      | ...   │  │
│  │ 3  | uuid-1          | ...     | EMP002      | ...   │  │
│  └───────────────────────────────────────────────────────┘  │
│  INDEX (organization_id, employee_id)                        │
│  UNIQUE (organization_id, employee_id)                       │
│                                                               │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ payroll_payrollrun                                    │  │
│  ├───────────────────────────────────────────────────────┤  │
│  │ id | organization_id | month | year | status | ...   │  │
│  ├────┼─────────────────┼───────┼──────┼────────┼───────┤  │
│  │ 1  | uuid-1          | 1     | 2026 | paid   | ...   │  │
│  │ 2  | uuid-2          | 1     | 2026 | paid   | ...   │  │
│  └───────────────────────────────────────────────────────┘  │
│  INDEX (organization_id, year, month)                        │
│                                                               │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ attendance_attendancerecord                           │  │
│  ├───────────────────────────────────────────────────────┤  │
│  │ id | organization_id | employee_id | date | ...       │  │
│  ├────┼─────────────────┼─────────────┼──────┼───────────┤  │
│  │ 1  | uuid-1          | 1           | ...  | ...       │  │
│  │ 2  | uuid-2          | 2           | ...  | ...       │  │
│  └───────────────────────────────────────────────────────┘  │
│  INDEX (organization_id, employee_id, date)                  │
│                                                               │
│  All other tables follow same pattern...                     │
└─────────────────────────────────────────────────────────────┘

Request Flow:
─────────────
1. HTTP Request: POST /api/v1/auth/login/
   Body: {"email": "admin@acme.com", "password": "..."}

2. Authentication:
   - User.objects.get(email="admin@acme.com", organization__slug="acme-corp")
   - Generate JWT token with organization_id

3. JWT Token:
   {
     "user_id": "...",
     "organization_id": "uuid-1",  ← 🔑 ISOLATION KEY
     "organization_slug": "acme-corp",  ← Display only
     "exp": ...
   }

4. Subsequent Requests: GET /api/employees/
   Headers: Authorization: Bearer <JWT>

5. Middleware:
   - Decode JWT → extract organization_id = "uuid-1"
   - set_current_organization(org_uuid_1)
   - (Optional) SET LOCAL app.current_organization_id = 'uuid-1'

6. View/QuerySet:
   - Employee.objects.all()
   - Manager auto-adds: .filter(organization_id=current_org.id)
   - Returns only ACME Corp employees

7. Database (with RLS):
   - Even if filter forgotten, PostgreSQL RLS blocks other orgs
   - WHERE organization_id::text = current_setting('app.current_organization_id')

Benefits:
─────────
✅ Single database, single schema
✅ Standard Django queries
✅ Cross-org analytics possible
✅ Single migration path
✅ No django-tenants dependency
✅ Works with any database
✅ Easier backups
✅ Better performance
```

---

## Data Isolation Enforcement (Multi-Layer Defense)

```
┌─────────────────────────────────────────────────────────────┐
│                      REQUEST ARRIVES                         │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│ Layer 1: Middleware (OrganizationMiddleware)                │
├─────────────────────────────────────────────────────────────┤
│ • Decode JWT token                                           │
│ • Extract organization_id from claims                        │
│ • Validate user belongs to organization                      │
│ • set_current_organization(org)                             │
│ • SET LOCAL app.current_organization_id (PostgreSQL RLS)    │
│                                                              │
│ ❌ No org context → 403 Forbidden                            │
│ ❌ Inactive org → 403 Forbidden                              │
│ ❌ Suspended subscription → 403 Forbidden                    │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│ Layer 2: Manager (OrganizationManager)                      │
├─────────────────────────────────────────────────────────────┤
│ • Employee.objects.all()                                     │
│ • get_current_organization() from contextvars               │
│ • Auto-add: .filter(organization=current_org)               │
│                                                              │
│ If org is None:                                             │
│   if ENVIRONMENT == 'production':                           │
│     ❌ raise RuntimeError("Data leak risk!")                │
│   else:                                                     │
│     ⚠️  log warning, return all data (dev only)            │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│ Layer 3: Permission Classes (IsOrganizationMember)          │
├─────────────────────────────────────────────────────────────┤
│ • has_object_permission(request, view, obj)                 │
│ • Check: obj.organization_id == user.organization_id        │
│                                                              │
│ ❌ Cross-org access → 403 Forbidden                          │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│ Layer 4: Serializer Validation                              │
├─────────────────────────────────────────────────────────────┤
│ • validate_department(department)                            │
│ • Check: department.organization == user.organization       │
│                                                              │
│ ❌ Cross-org reference → ValidationError                     │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│ Layer 5: Database (PostgreSQL RLS) ← OPTIONAL BUT POWERFUL  │
├─────────────────────────────────────────────────────────────┤
│ Policy:                                                      │
│   USING (organization_id::text =                            │
│          current_setting('app.current_organization_id'))    │
│                                                              │
│ Even if ALL above layers fail → Database blocks it!         │
│ ❌ Cross-org query → No rows returned                        │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                  ✅ DATA ISOLATED & SECURE                   │
└─────────────────────────────────────────────────────────────┘
```

---

## Context Management (Async-Safe)

```
┌─────────────────────────────────────────────────────────────┐
│              Threading.local (OLD) ❌                        │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Thread 1: Request A (acme-corp)                            │
│     _thread_locals.organization = org_uuid_1                │
│     ┌──────────────────────────────┐                        │
│     │ Sync View                    │                        │
│     │ Employee.objects.all()       │ ✅ Works              │
│     └──────────────────────────────┘                        │
│                                                              │
│  Thread 2: Request B (globex-inc)                           │
│     _thread_locals.organization = org_uuid_2                │
│     ┌──────────────────────────────┐                        │
│     │ Async View (async def)       │                        │
│     │ await Employee.objects.all() │ ❌ BREAKS!            │
│     └──────────────────────────────┘                        │
│     Problem: async tasks run in event loop, not threads    │
│     Context gets mixed or lost!                             │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│              Contextvars (NEW) ✅                            │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Request A (acme-corp) - Sync or Async                      │
│     current_organization_var.set(org_uuid_1)                │
│     ┌──────────────────────────────┐                        │
│     │ Sync View                    │                        │
│     │ Employee.objects.all()       │ ✅ Works              │
│     └──────────────────────────────┘                        │
│     ┌──────────────────────────────┐                        │
│     │ Async View (async def)       │                        │
│     │ await Employee.objects.all() │ ✅ Works!             │
│     └──────────────────────────────┘                        │
│                                                              │
│  Request B (globex-inc) - Sync or Async                     │
│     current_organization_var.set(org_uuid_2)                │
│     ┌──────────────────────────────┐                        │
│     │ WebSocket (Channels)         │                        │
│     │ Employee.objects.all()       │ ✅ Works!             │
│     └──────────────────────────────┘                        │
│     ┌──────────────────────────────┐                        │
│     │ Celery Task                  │                        │
│     │ set org → query              │ ✅ Works!             │
│     └──────────────────────────────┘                        │
│                                                              │
│  Context is properly isolated per request/task!             │
└─────────────────────────────────────────────────────────────┘
```

---

## Migration Process

```
Before Migration:
─────────────────
PostgreSQL Database
├── Schema: public (Tenant, Domain, Billing)
├── Schema: acme_corp (all tables)
├── Schema: globex_inc (all tables)
└── Schema: initech (all tables)

Step 1: Add organization_id columns (nullable)
───────────────────────────────────────────────
ALTER TABLE employees_employee ADD COLUMN organization_id UUID;
ALTER TABLE payroll_payrollrun ADD COLUMN organization_id UUID;
... (all tables)

Step 2: Run Data Migration
───────────────────────────
python manage.py migrate_to_organization_based --dry-run
python manage.py migrate_to_organization_based

For each tenant:
  1. Create Organization from Tenant
  2. SET search_path TO tenant_schema
  3. Update User.organization_id = org.id
  4. Update Employee.organization_id = org.id
  5. Update all other tables...

Step 3: Make organization_id NOT NULL
──────────────────────────────────────
ALTER TABLE employees_employee ALTER COLUMN organization_id SET NOT NULL;
... (all tables)

Step 4: Add Indexes & Constraints
──────────────────────────────────
CREATE INDEX idx_employee_org_id ON employees_employee(organization_id);
CREATE UNIQUE INDEX idx_employee_org_emp_id ON employees_employee(organization_id, employee_id);
... (all tables)

Step 5: Deploy New Code
────────────────────────
- Remove django-tenants
- Update middleware to OrganizationMiddleware
- Update settings (no TENANT_MODEL, no DATABASE_ROUTERS)

Step 6: Enable PostgreSQL RLS (Optional)
─────────────────────────────────────────
ALTER TABLE employees_employee ENABLE ROW LEVEL SECURITY;
CREATE POLICY org_isolation ON employees_employee ...
... (all tables)

After Migration:
────────────────
PostgreSQL Database (Single Schema)
└── Schema: public
    ├── core_organization (id, name, slug)
    ├── auth_user (id, email, organization_id)
    ├── employees_employee (id, organization_id, ...)
    ├── payroll_payrollrun (id, organization_id, ...)
    └── ... (all tables with organization_id)

Old schemas kept as backup for 1 week, then dropped.
```

---

## Key Differences Summary

| Aspect                  | Schema-Based ❌         | Organization-Based ✅   |
|-------------------------|-------------------------|-------------------------|
| **Isolation Method**    | PostgreSQL schemas      | organization_id FK      |
| **Tenant Resolution**   | Subdomain → schema      | JWT → organization_id   |
| **Database Structure**  | Multiple schemas        | Single schema           |
| **Migrations**          | Run per schema          | Run once                |
| **Cross-tenant queries**| Difficult               | Easy (JOIN on org_id)   |
| **Backup**              | Per schema              | Single dump             |
| **Django Queries**      | Auto-isolated by schema | Filter by org_id        |
| **Async Support**       | Complex                 | Native (contextvars)    |
| **Database Support**    | PostgreSQL only         | Any database            |
| **Complexity**          | High                    | Low                     |
| **Performance**         | Connection overhead     | Better                  |
| **Testing**             | Schema fixtures         | Standard fixtures       |

---

## Final Architecture Visualization

```
┌──────────────────────────────────────────────────────────────────────┐
│                        Production System                              │
└──────────────────────────────────────────────────────────────────────┘

                                   │
                    ┌──────────────┴──────────────┐
                    │   HTTPS Load Balancer        │
                    │   (hrms.yourcompany.com)     │
                    └──────────────┬──────────────┘
                                   │
           ┌───────────────────────┼───────────────────────┐
           │                       │                       │
     ┌─────▼─────┐          ┌─────▼─────┐          ┌─────▼─────┐
     │ Django 1  │          │ Django 2  │          │ Django 3  │
     │ (Gunicorn)│          │ (Gunicorn)│          │ (Gunicorn)│
     └─────┬─────┘          └─────┬─────┘          └─────┬─────┘
           │                       │                       │
           │ ┌─────────────────────┼───────────────────────┘
           │ │                     │
           │ │     ┌───────────────▼──────────────┐
           │ │     │ Middleware:                  │
           │ │     │ - JWT decode                 │
           │ │     │ - Extract organization_id    │
           │ │     │ - set_current_organization() │
           │ │     │ - SET LOCAL (RLS)            │
           │ │     └───────────────┬──────────────┘
           │ │                     │
           └─┴─────────────────────┼──────────────────────┐
                                   │                      │
                            ┌──────▼──────┐        ┌─────▼─────┐
                            │ PostgreSQL  │        │   Redis   │
                            │  (Single)   │        │  (Cache)  │
                            │  with RLS   │        └───────────┘
                            └─────────────┘
                                   │
                    Organization filtering enforced:
                    - Application: OrganizationManager
                    - Database: Row-Level Security
                    - API: Permission classes
                    - Serializers: Validation

Result: Fast, secure, maintainable SaaS architecture! 🚀
```
